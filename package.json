{
  "name": "kevin-portfolio",
  "version": "1.0.0",
  "description": "Personal portfolio for Kevin Darmawan — deployed on Cloudflare Workers",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
